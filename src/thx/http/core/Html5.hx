package thx.http.core;

import js.html.ArrayBuffer;
import js.html.ArrayBufferView;

class Html5 {
  // public static function arrayBufferViewToString(abv : ArrayBufferView) : String {
  //
  // }
  //
  // public static function arrayBufferViewToString(abv : ArrayBufferView) : String {
  //
  // }

  //
  // public static function bufferToArrayBuffer(buf : Buffer) : js.html.ArrayBuffer {
  //   var ab = new js.html.ArrayBuffer(buf.length);
  //   var view = new js.html.Uint8Array(ab);
  //   for(i in 0...buf.length)
  //     view[i] = buf[i];
  //   return ab;

  // static function arrayBufferToString(ab : js.html.ArrayBuffer) : String {
  //   return (untyped __js__("String")).fromCharCode.apply(null, new js.html.Uint16Array(ab));
  // }
  //
  // public static function arrayBufferToBuffer(ab : js.html.ArrayBuffer) : Buffer {
  //   var buffer = new Buffer(ab.byteLength);
  //   var view = new js.html.Uint8Array(ab);
  //   for(i in 0...buffer.length)
  //     buffer[i] = view[i];
  //   return buffer;
  // }
}
